# Default ALSA config — routes all audio through the PulseAudio plugin
# to PipeWire's pipewire-pulse server. Per-speaker PCMs (bt_<mac>) are
# added dynamically by the bridge at runtime.

pcm.!default {
    type pulse
}

ctl.!default {
    type pulse
}
